name: Hello workflow

on: 
  push:
    branches:
      - main
    # paths-ignore:
    #   - '.github/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Choose environment'
        type: environment
        required: true

jobs:
  tmp:
    runs-on: ubuntu-latest
    env:
      ECR_ROLE: ${{ secrets.TMP }}
    steps:
      - name: Dump TMP
        run: |
          echo TMP is $TMP
          echo tmp = ${{ secrets.TMP }}
          echo env


  # be-setup-dev:
  #   # if: ${{ inputs.environment == '' || inputs.environment == 'dev' }}
  #   name: Hello dev job
  #   uses: ./.github/workflows/reusable-flow.yml
  #   with:
  #     environment: 'dev'
  #   secrets:
  #     ECR_ROLE: ${{ secrets.ECR_ROLE }}
  #   # secrets: inherit
  # be-setup-prod:
  #   # if: inputs.environment == 'prod'
  #   name: Hello prod job
  #   uses: ./.github/workflows/reusable-flow.yml
  #   with:
  #     environment: 'prod'
  #   secrets:
  #     ECR_ROLE: ${{ secrets.ECR_ROLE }}
    